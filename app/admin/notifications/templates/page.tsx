// P20.5G: template authoring/versioning is served entirely by the existing
// Business Configuration 'notifications' category editor (P20.5D) — reused
// as-is rather than duplicated, per the platform's "reuse Business
// Configuration versioning" requirement.
export { default } from "../../settings/notifications/page";
