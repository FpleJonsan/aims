import {Body,Controller,Get,Param,ParseUUIDPipe,Post,Req,UseGuards} from '@nestjs/common';import type {Request} from 'express';import {AuthGuard} from '../auth/auth.guard.js';import {FinalizeFinancialAnalysisDto} from './financial-analysis.dto.js';import {FinancialAnalysisService} from './financial-analysis.service.js';
@UseGuards(AuthGuard) @Controller('payment-requests/:id/financial-analysis') export class FinancialAnalysisController{constructor(private readonly service:FinancialAnalysisService){}@Post()start(@Req()r:Request,@Param('id',ParseUUIDPipe)id:string){return this.service.start(id,r.principal,r.correlationId)}@Post('manual')manual(@Req()r:Request,@Param('id',ParseUUIDPipe)id:string,@Body()b:FinalizeFinancialAnalysisDto){return this.service.manual(id,b,r.principal,r.correlationId)}@Post(':analysisId/finalize')finalize(@Req()r:Request,@Param('id',ParseUUIDPipe)id:string,@Param('analysisId',ParseUUIDPipe)analysisId:string,@Body()b:FinalizeFinancialAnalysisDto){return this.service.finalize(id,analysisId,b,r.principal,r.correlationId)}@Get()get(@Req()r:Request,@Param('id',ParseUUIDPipe)id:string){return this.service.get(id,r.principal,false)}@Get('history')history(@Req()r:Request,@Param('id',ParseUUIDPipe)id:string){return this.service.get(id,r.principal,true)}}
