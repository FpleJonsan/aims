export const FINANCE_CONTEXT_CALCULATION_VERSION='finance-context-v1';
export type FinanceContextException='MISSING_APPLICABLE_BUDGET'|'INACTIVE_BUDGET'|'AMBIGUOUS_BUDGET_MAPPING'|'CURRENCY_CONTEXT_UNSUPPORTED'|'STALE_VALIDATION'|'INVALID_REQUEST_AMOUNT'|'INCONSISTENT_BUDGET_DATA';
export function decimalToMinor(value:string,precision=2):bigint{if(!/^\d+(\.\d+)?$/.test(value))throw new Error('INVALID_REQUEST_AMOUNT');const [whole,fraction='']=value.split('.');if(fraction.length>precision&&/[1-9]/.test(fraction.slice(precision)))throw new Error('INVALID_REQUEST_AMOUNT');return BigInt(whole)*10n**BigInt(precision)+BigInt((fraction.slice(0,precision)+'0'.repeat(precision)).slice(0,precision))}
export function calculateBudget(revised:bigint,actual:bigint,committed:bigint,request:bigint){if([revised,actual,committed,request].some(value=>value<0n))throw new Error('INCONSISTENT_BUDGET_DATA');const available=revised-actual-committed;return {available,projectedAvailable:available-request}}
export function minorToDecimal(value:string|bigint,precision=2){const amount=BigInt(value);const negative=amount<0n;const absolute=negative?-amount:amount;const base=10n**BigInt(precision);return `${negative?'-':''}${absolute/base}.${String(absolute%base).padStart(precision,'0')}`}
